# Core ML Framework
torch>=2.0.0
# torchaudio — install separately to match torch version:
#   pip install torch torchaudio --extra-index-url https://download.pytorch.org/whl/cu121

# Transformers & Model Loading
transformers>=4.40.0
huggingface_hub>=0.22.0

# Audio Processing
librosa>=0.10.0
soundfile>=0.12.0
numpy>=1.24.0

# Core TTS Models
neucodec>=0.0.4
sea-g2p>=0.1.0

# Fast Mode (GPU/LMDeploy) - optional but recommended for GPU
lmdeploy>=0.1.0
